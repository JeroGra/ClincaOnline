import { DColorBtnDirective } from './d-color-btn.directive';

describe('DColorBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new DColorBtnDirective();
    expect(directive).toBeTruthy();
  });
});
