import { ClickActionDirective } from './click-action.directive';

describe('ClickActionDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickActionDirective();
    expect(directive).toBeTruthy();
  });
});
