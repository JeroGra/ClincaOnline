import { ColorHoverDirective } from './color-hover.directive';

describe('ColorHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorHoverDirective();
    expect(directive).toBeTruthy();
  });
});
