import { EnterActionDirective } from './enter-action.directive';

describe('EnterActionDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterActionDirective();
    expect(directive).toBeTruthy();
  });
});
