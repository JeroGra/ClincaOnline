import { DirectivaFiltroDinamicoTurnoDirective } from './directiva-filtro-dinamico-turno.directive';

describe('DirectivaFiltroDinamicoTurnoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaFiltroDinamicoTurnoDirective();
    expect(directive).toBeTruthy();
  });
});
